package com.unityskill.tracking;

public enum ResourceType {
    /** GitHub repository identified by "owner/repo" string. */
    GITHUB_REPO,
    /** Chat channel identified by projectId UUID string. */
    CHAT_CHANNEL
}
