package com.unityskill.privacy.entity;

public enum ExportStatus {
    /** Export job is running asynchronously. */
    IN_PROGRESS,
    /** Export JSON is compiled and ready for download. */
    READY
}
